import React from 'react'

export default function Test() {
    return <div>This is a test page to test design components or functions without affecting the main app. when adding something never delete this div and just replace everything with the tests just not this div and its content
  </div>
}